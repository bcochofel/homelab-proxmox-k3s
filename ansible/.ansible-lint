---
profile: basic

# var-naming[no-role-prefix] wants every role variable prefixed with the
# role's own name. This project instead uses a leading underscore for
# internal/registered facts (_docker_check, _compose_check, ...), matching
# the sibling homelab-proxmox-elastic repo's convention (see its CLAUDE.md).
skip_list:
  - var-naming[no-role-prefix]
