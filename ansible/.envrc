source_up

export ANSIBLE_CONFIG="${PWD}/ansible.cfg"
# SOPS_AGE_KEY_FILE inherited from root.

export CLOUDFLARE_API_TOKEN="${cloudflare_api_token}"
export FLEET_ENROLLMENT_TOKEN="${elastic_fleet_enrollment_token}"
